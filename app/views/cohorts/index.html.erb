<h1>Cohorts#index</h1>
<p>Find me in app/views/cohorts/index.html.erb</p>

<% @instructor.each do |c| %>
  <div class="cohort" id="<%= c.id %>">
    <%= c.name %> <br>
    <%= c.course_id %> <br>
    <%= c.start_date %> to <%= c.end_date %><br>
    <%= link_to 'Edit Cohort', edit_cohort_path(c) %>
    <%# link_to c.name, course_cohort_path(c) %>
    
  </div>
<% end %>